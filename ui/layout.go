package ui

// NarrowWidthThreshold is the shared width (in columns) below which panels
// switch to a more compact layout: split views stack horizontally instead of
// vertically, and list rows render in a condensed style.
const NarrowWidthThreshold = 100
