{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/elentok/gx/main/docs/config-schema.json",
  "title": "gx config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "use-nerdfont-icons": {
      "type": "boolean",
      "description": "Enable Nerd Font icons in the UI. Default: true.",
      "default": true
    },
    "image-diffs": {
      "type": "boolean",
      "description": "Render inline image diffs (via the kitty graphics protocol) in the status diff panel on supported terminals. Default: true.",
      "default": true
    },
    "stage-diff-context-lines": {
      "type": "integer",
      "description": "Number of context lines shown around each diff hunk in the staging view. Clamped to 0–20. Default: 1.",
      "minimum": 0,
      "maximum": 20,
      "default": 1
    },
    "input-modal-bottom": {
      "description": "Vertical position of the text-input overlay. An integer means fixed lines from the bottom, a percentage string (e.g. \"5%\") means a fraction of screen height from the bottom, and \"center\" places it in the middle of the screen. Default: \"5%\".",
      "default": "5%",
      "oneOf": [
        {
          "type": "integer",
          "minimum": 0,
          "description": "Fixed number of lines from the bottom of the screen."
        },
        {
          "type": "string",
          "pattern": "^\\d+(\\.\\d+)?%$",
          "description": "Percentage of screen height from the bottom (e.g. \"5%\")."
        },
        {
          "type": "string",
          "enum": ["center"],
          "description": "Center the overlay vertically on the screen."
        }
      ]
    },
    "name-aliases": {
      "type": "object",
      "description": "Exact full-name aliases used for kitty session names.",
      "additionalProperties": {
        "type": "string"
      },
      "default": {}
    },
    "execution-queue": {
      "type": "object",
      "description": "Execution queue concurrency limits.",
      "additionalProperties": false,
      "properties": {
        "max-concurrent-tickets-per-epic": {
          "type": "integer",
          "description": "Maximum tickets that may run concurrently within one epic. Default: 2.",
          "minimum": 1,
          "default": 2
        },
        "max-concurrent-epics": {
          "type": "integer",
          "description": "Maximum epics that may run concurrently in this gx process. Default: 2.",
          "minimum": 1,
          "default": 2
        }
      },
      "default": {
        "max-concurrent-tickets-per-epic": 2,
        "max-concurrent-epics": 2
      }
    },
    "log": {
      "type": "object",
      "description": "Log view display settings.",
      "additionalProperties": false,
      "properties": {
        "important-refs": {
          "type": "array",
          "description": "Rules for highlighting important refs in the log view. Refs matching a rule get a bright colored badge; all others get a dim surface badge. Rules are evaluated in order — the first match wins. The order also controls sort priority: refs matching earlier rules appear first.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["patterns", "color"],
            "properties": {
              "patterns": {
                "type": "array",
                "description": "Regular expressions matched against the full ref name (e.g. \"^main$\", \"^origin/main$\", \"^v\\\\d\").",
                "items": { "type": "string" },
                "minItems": 1
              },
              "color": {
                "type": "string",
                "description": "Badge color. Named Catppuccin colors: blue, green, yellow, orange, mauve, teal, red, surface. Or a hex value: #rrggbb or #rgb."
              }
            }
          },
          "default": [
            { "patterns": ["^main$", "^master$", "^origin/main$", "^origin/master$"], "color": "yellow" }
          ]
        },
        "hide-refs": {
          "type": "array",
          "description": "Regular expressions matched against the full ref name. Matching refs are hidden from the log view entirely. Takes priority over important-refs. Default: empty (nothing hidden).",
          "items": { "type": "string" },
          "default": []
        }
      }
    },
    "notifications": {
      "type": "object",
      "description": "External notification integrations.",
      "additionalProperties": false,
      "properties": {
        "telegram": {
          "type": "object",
          "description": "Telegram bot credentials for sending notifications. An empty bot-token disables Telegram notifications.",
          "additionalProperties": false,
          "properties": {
            "bot-token": {
              "type": "string",
              "description": "Telegram bot token. Default: empty (disabled).",
              "default": ""
            },
            "chat-id": {
              "type": "string",
              "description": "Telegram chat id to send notifications to. Default: empty.",
              "default": ""
            }
          },
          "default": {
            "bot-token": "",
            "chat-id": ""
          }
        }
      },
      "default": {
        "telegram": {
          "bot-token": "",
          "chat-id": ""
        }
      }
    },
    "skills": {
      "type": "object",
      "description": "Which skill(s) gx-implement-equivalent work and code review run under.",
      "additionalProperties": false,
      "properties": {
        "implement": {
          "type": "string",
          "description": "Skill invoked for ticket-implementation work. Default: gx-implement.",
          "default": "gx-implement"
        },
        "code-review": {
          "type": "array",
          "description": "Skill(s) invoked for code review. Default: [thermo-nuclear-code-quality-review].",
          "items": { "type": "string" },
          "default": ["thermo-nuclear-code-quality-review"]
        }
      },
      "default": {
        "implement": "gx-implement",
        "code-review": ["thermo-nuclear-code-quality-review"]
      }
    }
  }
}
