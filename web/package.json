{
  "name": "gx-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Landing site for gx — renders as a faux-TUI gx screen.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=gx"
  },
  "dependencies": {
    "preact": "^10.27.2",
    "preact-iso": "^2.9.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.12.3",
    "typescript": "~5.9.3",
    "vite": "^7.1.12",
    "wrangler": "^4.43.0"
  }
}
